[pytest]
# basetemp pinned outside the FUSE mount: the mount refuses `unlink`, so
# pytest's default tempdir teardown recurses forever when run inside an
# agent sandbox. Harmless on CI (ordinary tmpfs there). Background:
# design/soft-delete-convention.md.
addopts = --basetemp=/tmp/pytest-kids
