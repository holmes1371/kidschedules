[pytest]
# basetemp pinned outside the working tree as a defensive carry-over —
# was originally needed when the local mount refused unlink. Harmless on
# CI (ordinary tmpfs there); safe to drop the addopts line if local
# pytest runs work without it.
addopts = --basetemp=/tmp/pytest-kids
